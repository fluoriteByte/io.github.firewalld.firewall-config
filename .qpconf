#!/bin/bash
##### QuickPak config file
##### https://codeberg.org/fluoriteByte/quickpak/

APP_ID=org.firewalld.firewall-config
MANIFEST=$APP_ID.yaml

source_gen() {
  #No longer needed!
  true
}

echo "$FPBUILD $APP_ID $MANIFEST $METAINFO $LINT_FLAG $BUILD_FLAG"
